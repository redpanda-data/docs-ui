<main class="article{{#with (or page.attributes.role page.role)}} {{{this}}}{{/with}}">
  {{> toolbar}}
  <div class="content">
    {{#if (eq page.layout '404')}}
    {{> article-404}}
    {{else if (eq page.attributes.role 'home')}}
    {{> article}}
    {{else}}
    {{> toc}}
    {{> article}}
    {{/if}}
  </div>
  {{#if (ne page.attributes.role 'home')}}
  {{> footer}}
  {{/if}}
</main>
